server:
  port: 8088

spring:
  application:
    name: creditcard-microservice

  config:
    import: optional:configserver:https://azurespringboot2022-configserver.azuremicroservices.io
  profiles:
    active: dev
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

